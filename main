--[[
    👑 SOVEREIGN V46+ | BROOKHAVEN AI ULTIMATE
    Özellik: Brookhaven Şehir Tetikleyici & Yapay Zeka Destekli Stabilizasyon
    Durum: Full Hata Düzeltildi
]]

-- GLOBAL AYARLAR (Hata önleyici varsayılanlar)
_G.Speed = 16 [cite: 3]
_G.Jump = 50 [cite: 3]
_G.FOV = 70 [cite: 3]
_G.Noclip = false [cite: 3]
_G.InfJump = false [cite: 3]

-- HİZMETLER
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local lp = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- AI TABANLI OTOMATİK DÜZELTME (Karakter yenilendiğinde hileyi tekrar bağlar)
local function AI_Enforce(char)
    if not char then return end
    local hum = char:WaitForChild("Humanoid", 5)
    if hum then
        hum.WalkSpeed = _G.Speed [cite: 7, 42]
        hum.JumpPower = _G.Jump [cite: 8, 43]
        hum.UseJumpPower = true [cite: 8, 43]
    end
end
lp.CharacterAdded:Connect(AI_Enforce) [cite: 44]

-- UI YÜKLEME (Sirius Rayfield - Hata Korumalı)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))() [cite: 2]

local Window = Rayfield:CreateWindow({
   Name = "👑 SOVEREIGN AI | BROOKHAVEN",
   LoadingTitle = "Yapay Zeka Sistemleri Başlatılıyor...",
   LoadingSubtitle = "Brookhaven Şehri Tetikleniyor",
   ConfigurationSaving = { Enabled = false }
}) [cite: 5]

-- TABS
local MainTab = Window:CreateTab("🏠 Ana Kontrol", 4483362458)
local BrookTab = Window:CreateTab("🏙️ Brookhaven Şehir", 4483362458)
local MoveTab = Window:CreateTab("⚡ Hareket", 4483362458)

-- BROOKHAVEN ŞEHİR TETİKLEYİCİLERİ (AI Destekli Arama)
BrookTab:CreateSection("Şehir Etkileşimleri")

BrookTab:CreateButton({
   Name = "🔓 Tüm Kapıları/Kilitleri Zorla (AI Scan)",
   Callback = function()
       -- Şehirdeki tüm etkileşimli nesneleri bulur ve tetikler
       for _, v in pairs(workspace:GetDescendants()) do
           if v:IsA("ClickDetector") or v.Name:lower():match("door") or v.Name:lower():match("lock") then
               fireclickdetector(v) -- Eğer exploit destekliyorsa
           end
       end
       Rayfield:Notify({Title="AI Tarama", Content="Şehirdeki kilitler tetiklendi.", Duration=3})
   end,
})

BrookTab:CreateButton({
   Name = "💰 Banka Soygunu Hazırla (TP + Bomb)",
   Callback = function()
       pcall(function()
           -- Banka Kasası Konumu
           lp.Character.HumanoidRootPart.CFrame = CFrame.new(-10.733, 15.452, 275.049) [cite: 24]
           task.wait(0.5)
           -- Brookhaven Remote trigger (Eşya alımı)
           local remote = ReplicatedStorage:FindFirstChild("ItemRemote", true) [cite: 26]
           if remote then remote:FireServer("C4") end
       end)
   end,
})

-- HAREKET AYARLARI
MoveTab:CreateSlider({
   Name = "Yürüme Hızı",
   Range = {16, 500},
   CurrentValue = 16,
   Callback = function(v) _G.Speed = v end,
}) [cite: 10]

MoveTab:CreateToggle({
   Name = "Noclip (Duvar Geçme)",
   CurrentValue = false,
   Callback = function(v) _G.Noclip = v end,
}) [cite: 10]

MoveTab:CreateToggle({
   Name = "Sonsuz Zıplama",
   CurrentValue = false,
   Callback = function(v) _G.InfJump = v end,
}) [cite: 10]

-- ARKA PLAN DÖNGÜSÜ (Heartbeat - Hiç Durmaz)
RunService.Heartbeat:Connect(function()
    pcall(function()
        if lp.Character and lp.Character:FindFirstChild("Humanoid") then
            lp.Character.Humanoid.WalkSpeed = _G.Speed [cite: 45]
            lp.Character.Humanoid.JumpPower = _G.Jump [cite: 45]
            
            if _G.Noclip then
                for _, part in pairs(lp.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end [cite: 45]
                end
            end
        end
    end)
end) [cite: 45]

-- JUMP REQUEST (Sonsuz Zıplama)
UserInputService.JumpRequest:Connect(function()
    if _G.InfJump and lp.Character:FindFirstChild("Humanoid") then
        lp.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end) [cite: 46]

Rayfield:Notify({
   Title = "HİLE AKTİF",
   Content = "Brookhaven AI Edition Hatasız Yüklendi!",
   Duration = 5
}) [cite: 53]
