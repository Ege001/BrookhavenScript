--[[ 
    👑 SOVEREIGN V46 | BROOKHAVEN ADS & DASHBOARD EDITION
    OWNER: EgeKazak_Real1
    STATUS: FULL INTEGRATED + ADS MENU BYPASS
]]

-- 1. BAŞLATICI VE GÜVENLİK
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local lp = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- EKRAN HUD (ANA SAYAÇ & DURUM)
local function CreateHUD()
    if game.CoreGui:FindFirstChild("SOV_HUD") then game.CoreGui.SOV_HUD:Destroy() end
    local sg = Instance.new("ScreenGui", game.CoreGui)
    sg.Name = "SOV_HUD"
    
    local frame = Instance.new("Frame", sg)
    frame.Size = UDim2.new(0, 200, 0, 80)
    frame.Position = UDim2.new(0.02, 0, 0.8, 0)
    frame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
    frame.BorderSizePixel = 2
    
    local title = Instance.new("TextLabel", frame)
    title.Size = UDim2.new(1, 0, 0, 25)
    title.Text = "SOVEREIGN DASHBOARD"
    title.TextColor3 = Color3.fromRGB(0, 170, 255)
    title.BackgroundTransparency = 1
    title.Font = Enum.Font.GothamBold
    
    local stats = Instance.new("TextLabel", frame)
    stats.Name = "StatLabel"
    stats.Size = UDim2.new(1, 0, 1, -25)
    stats.Position = UDim2.new(0, 0, 0, 25)
    stats.Text = "Hız: 16\nSunucu: " .. #Players:GetPlayers()
    stats.TextColor3 = Color3.fromRGB(255, 255, 255)
    stats.BackgroundTransparency = 1
    stats.TextSize = 14

    task.spawn(function()
        while task.wait(0.5) do
            if lp.Character and lp.Character:FindFirstChild("Humanoid") then
                stats.Text = "Hız: " .. math.floor(lp.Character.Humanoid.WalkSpeed) .. "\nSunucu: " .. #Players:GetPlayers() .. " Kişi"
            end
        end
    end)
end
pcall(CreateHUD)

-- 2. UI KÜTÜPHANESİ
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- GLOBAL AYARLAR
_G.Speed = 16
_G.Jump = 50
_G.Noclip = false
_G.AdsMessage = "SOVEREIGN V46 ON TOP!"

local Window = Rayfield:CreateWindow({
   Name = "👑 SOVEREIGN V46 | BROOKHAVEN ADS",
   LoadingTitle = "EgeKazak_Real1 Dashboard",
   LoadingSubtitle = "Ads Menu & Trigger Systems",
   Theme = "DarkBlue",
   ConfigurationSaving = { Enabled = false }
})

-- SEKMELER
local DashTab = Window:CreateTab("📊 Ana Panel", 4483362458)
local AdsTab = Window:CreateTab("📢 Ads Menu", 4483362458)
local MoveTab = Window:CreateTab("⚡ Hareket", 4483362458)
local TPTab = Window:CreateTab("✈️ Konumlar", 4483362458)

-- 3. ANA PANEL (DASHBOARD) ÖZELLİKLERİ
DashTab:CreateSection("Sunucu Bilgileri")
DashTab:CreateLabel("Kullanıcı: " .. lp.Name)
DashTab:CreateLabel("Hesap Yaşı: " .. lp.AccountAge .. " Gün")

DashTab:CreateButton({
    Name = "🛠️ Tüm Menüleri Tetikle (Trigger All)",
    Callback = function()
        -- Oyundaki tüm etkileşimli eventleri tetiklemeye çalışır
        for _,v in pairs(game:GetDescendants()) do
            if v:IsA("RemoteEvent") and (v.Name:find("Click") or v.Name:find("Trigger")) then
                v:FireServer()
            end
        end
        Rayfield:Notify({Title="Tetiklendi", Content="Tüm RemoteEvent'ler zorlandı!", Duration=3})
    end
})

-- 4. ADS MENU (REKLAM PANOSU) MANİPÜLASYONU
AdsTab:CreateSection("Pano Ayarları")

AdsTab:CreateInput({
   Name = "Pano Yazısı Değiştir",
   PlaceholderText = "Yazı yazın...",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
       _G.AdsMessage = Text
   end,
})

AdsTab:CreateButton({
   Name = "📢 Tüm Reklamları Güncelle",
   Callback = function()
       pcall(function()
           -- Brookhaven Pano Eventi (Ads Remote)
           local remote = ReplicatedStorage:FindFirstChild("UpdateAds", true) or ReplicatedStorage:FindFirstChild("AdsRemote", true)
           if remote then
               remote:FireServer(_G.AdsMessage)
           else
               Rayfield:Notify({Title="Hata", Content="Ads Remote bulunamadı!", Duration=3})
           end
       end)
   end,
})

AdsTab:CreateButton({
    Name = "🚫 Reklamları Temizle",
    Callback = function()
        local remote = ReplicatedStorage:FindFirstChild("UpdateAds", true)
        if remote then remote:FireServer("") end
    end
})

-- 5. HAREKET KONTROLLERİ
MoveTab:CreateSlider({
   Name = "Yürüme Hızı",
   Range = {16, 500},
   CurrentValue = 16,
   Callback = function(v) _G.Speed = v end,
})

MoveTab:CreateToggle({
   Name = "Noclip (Duvar Geçme)",
   Callback = function(v) _G.Noclip = v end,
})

-- 6. BROOKHAVEN KONUMLAR (FULL)
local locs = {
    ["💰 Banka Kasası"] = CFrame.new(-10.7, 15.4, 275.0),
    ["🚓 Polis Hücresi"] = CFrame.new(-130.5, 3.0, 20.5),
    ["🏠 Gizli Göl Evi"] = CFrame.new(-485.6, 23.5, 680.9),
    ["🚢 Lüks Yat"] = CFrame.new(-380.1, 15.2, -420.5),
    ["🏥 Hastane Gizli Oda"] = CFrame.new(-165.2, 20.5, 50.3)
}

for name, cf in pairs(locs) do
    TPTab:CreateButton({
        Name = name,
        Callback = function()
            if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                lp.Character.HumanoidRootPart.CFrame = cf
            end
        end
    })
end

-- 7. SİSTEM DÖNGÜSÜ
RunService.Heartbeat:Connect(function()
    pcall(function()
        if lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") then
            local h = lp.Character:FindFirstChildOfClass("Humanoid")
            h.WalkSpeed = _G.Speed
            h.JumpPower = _G.Jump
            h.UseJumpPower = true
        end
        if _G.Noclip and lp.Character then
            for _,part in pairs(lp.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)
end)

Rayfield:Notify({
    Title = "Sovereign ADS Edition",
    Content = "Tüm Dashboard ve Reklam sistemleri aktif!",
    Duration = 5
})
