-- Modern UI & AI Core
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local lp = game.Players.LocalPlayer
local runService = game:GetService("RunService")
local camera = workspace.CurrentCamera
local rs = game:GetService("ReplicatedStorage")
local ownerName = "EgeKazak_Real1"

-- SİSTEM DEĞİŞKENLERİ
local flying, noclip = false, false
local flySpeed, walkSpeed, jumpPower = 50, 16, 50
local viewTarget, rainbowHouses = nil, false

-- 1. WINDOW OLUŞTURMA
local Window = Rayfield:CreateWindow({
   Name = "👑 Gemini AI V28 | OWNER: " .. ownerName,
   LoadingTitle = "Sistem Verileri Optimize Ediliyor...",
   LoadingSubtitle = "EgeKazak_Real1 Mutlak Yetki Paneli",
   ConfigurationSaving = { Enabled = true, FolderName = "GeminiV28" }
})

-- SEKMELER
local MasterTab = Window:CreateTab("Owner Control", 4483362458)
local ViewTab = Window:CreateTab("Eşya Seçici (View)", 4483362458)
local PlayerTab = Window:CreateTab("Oyuncu (View/Copy)", 4483362458)
local VehicleTab = Window:CreateTab("Araç & RGB", 4483362458)
local WorldTab = Window:CreateTab("Dünya & Evler", 4483362458)
local MoveTab = Window:CreateTab("Hareket (Fly)", 4483362458)
local SettingsTab = Window:CreateTab("Ayarlar (Dil)", 4483362458)

-- --- MASTER: OWNER ALL GAMEPASS ---
MasterTab:CreateButton({
   Name = "🔥 OWNER ALL GAMEPASS & ADS (EgeKazak_Real1)",
   Callback = function()
      pcall(function()
          -- PlayersBag Unlock
          local pb = lp:FindFirstChild("PlayersBag") or lp:WaitForChild("PlayersBag", 5)
          for _, v in pairs(pb:GetDescendants()) do
              if v:IsA("BoolValue") then v.Value = true
              elseif v:IsA("NumberValue") then v.Value = 1 end
          end
          -- AllPassesFetched & GC Bypass
          for _, v in pairs(getgc(true)) do
              if type(v) == "table" and rawget(v, "AllPassesFetched") ~= nil then
                  v.AllPassesFetched = true
                  v.Premium, v.VIP, v.Unlocked = true, true, true
              end
          end
          -- Ads Button Fix
          local MainGUI = lp.PlayerGui:FindFirstChild("MainGUIHandler", true)
          if MainGUI then
              local ads = MainGUI:FindFirstChild("AdStore", true)
              if ads then ads.Visible = true; ads.Position = UDim2.new(0.5, 0, 0.5, 0) end
          end
          Rayfield:Notify({Title = "AI Onay", Content = "Tüm kilitler EgeKazak_Real1 için açıldı!", Duration = 5})
      end)
   end,
})

-- --- EŞYA SEÇİCİ (VIEW/SPAWN) ---
local itemNames = {}
for _, t in pairs(rs:GetDescendants()) do if t:IsA("Tool") then table.insert(itemNames, t.Name) end end
ViewTab:CreateDropdown({
   Name = "Brookhaven Tüm Eşyalar", Options = itemNames,
   Callback = function(Option)
       local tool = rs:FindFirstChild(Option, true)
       if tool then tool:Clone().Parent = lp.Backpack end
   end,
})

-- --- PLAYER: VIEW & COPY AVATAR ---
PlayerTab:CreateInput({
   Name = "Oyuncu Ara (İsim)", PlaceholderText = "Ege...",
   Callback = function(Text)
       for _, p in pairs(game.Players:GetPlayers()) do
           if p.Name:lower():find(Text:lower()) then viewTarget = p end
       end
   end,
})
PlayerTab:CreateToggle({
   Name = "View (İzle)", CurrentValue = false,
   Callback = function(v) camera.CameraSubject = (v and viewTarget) and viewTarget.Character.Humanoid or lp.Character.Humanoid end,
})
PlayerTab:CreateButton({
   Name = "Avatar Kopyala (Copy Avatar)",
   Callback = function() if viewTarget then lp.CharacterAppearanceId = viewTarget.UserId; lp.Character:BreakJoints() end end,
})

-- --- VEHICLE: RGB & TURBO ---
VehicleTab:CreateToggle({
   Name = "Araç Turbo & RGB Mod", CurrentValue = false,
   Callback = function(Value)
      _G.CarMod = Value
      spawn(function()
         while _G.CarMod do
            for _, v in pairs(workspace.Vehicles:GetChildren()) do
                local seat = v:FindFirstChildWhichIsA("VehicleSeat", true)
                if seat and seat.Occupant and seat.Occupant.Parent == lp.Character then
                    seat.MaxSpeed, seat.Torque = 500, 50000
                    if v:FindFirstChild("Body") then v.Body.Color = Color3.fromHSV(tick() % 5 / 5, 1, 1) end
                end
            end
            task.wait(0.1)
         end
      end)
   end,
})

-- --- WORLD: RAINBOW HOUSES ---
WorldTab:CreateToggle({
   Name = "Rainbow Evler (Gökkuşağı)", CurrentValue = false,
   Callback = function(v)
       rainbowHouses = v
       spawn(function()
           while rainbowHouses do
               for _, house in pairs(workspace.Houses:GetChildren()) do
                   for _, part in pairs(house:GetDescendants()) do
                       if part:IsA("BasePart") and part.Name == "HouseColor" then
                           part.Color = Color3.fromHSV(tick() % 5 / 5, 1, 1)
                       end
                   end
               end
               task.wait(0.1)
           end
       end)
   end,
})

-- --- MOVEMENT: FLY / NOCLIP ---
MoveTab:CreateToggle({Name = "Fly (Uçma)", Callback = function(v) flying = v end})
MoveTab:CreateSlider({Name = "Hız", Range = {16, 500}, Callback = function(v) lp.Character.Humanoid.WalkSpeed = v end})
MoveTab:CreateToggle({
    Name = "Noclip (Duvar Geçme)",
    Callback = function(v)
        noclip = v
        runService.Stepped:Connect(function()
            if noclip and lp.Character then
                for _, part in pairs(lp.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end)
    end,
})

-- --- SETTINGS: LANGUAGE & DESTROY ---
SettingsTab:CreateDropdown({
   Name = "Dil Seçimi (Language)", Options = {"Türkçe", "English", "Deutsch", "Español"},
   Callback = function(s) Rayfield:Notify({Title = "Sistem", Content = "Dil: " .. s}) end,
})
SettingsTab:CreateButton({
   Name = "🔴 SCRIPT DESTROY (TAMAMEN KAPAT)",
   Callback = function() Rayfield:Destroy() end,
})
